func pow(x: integer, p: integer)->integer{
   let i: integer;
   let j: integer;

   i=0;
   j=x;

   if(p==1) then {
    return(x);
    }else {
      while(i<p){
          x=x*j;
          i=i+1;
        };
        return(x);
      };
  }

func main()->void{
  let i: integer;
  while(i<10){
    write(pow(2,i));
    i=i+1;
    };

  }
