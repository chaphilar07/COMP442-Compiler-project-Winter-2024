NOTE:

 For class access, the dot chain basically becomes a SEQUENCE OF OFFSETS INTO THE CURRENT STACK FRAMES MEMORY FRAME!!!!
 SO we can keep some running sum access the memory and 


We can use a "heap" like region of memory, this region of memory we will use to store the results of function's, we can then get this value for the return of a functions we can also use this region for passing parameters if we have more than 12 parameters,
we reserve 248 bytes for this region this should be neough to return small arrays and 

We are going to need functions that are going to do the following:

1. we need to be able to calculate the offset of class objects and arrays.
Note that we never ever change the stack frame!! this is very convenient!

 1. find the offset of the variable or function call that starts the dot chain.
 2. go down the right, while we can go down the right we add to a running sum by adding the size of that variable in the classes offset.
